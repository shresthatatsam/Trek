@* @using UserRoles.Dtos.ResponseDtos;
@{
    var carouselImages = ViewBag.CarouselImages as List<CarousalImageResponseDto>;
}


@await Html.PartialAsync("_Navbar")
@await Html.PartialAsync("_Carousel", carouselImages)  *@

@model AboutUs

<h2>@Model.Title</h2>
<p><strong>Mission:</strong> @Model.Mission</p>
<p><strong>Story:</strong> @Model.Story</p>

<h3>Team Members</h3>
@* <ul>
    @foreach (var member in Model.TeamMembers)
    {
        <li>
            <img src="@member.PhotoUrl" alt="@member.Name" width="50" />
            <strong>@member.Name</strong> - @member.Role
            <p>@member.Bio</p>

            <form asp-action="DeleteTeamMember" method="post">
                <input type="hidden" name="id" value="@member.Id" />
                <button type="submit">Delete</button>
            </form>
        </li>
    }
</ul> *@

<h4>Add Team Member</h4>
<form asp-action="AddTeamMember" method="post">
    <input type="hidden" name="AboutUsSectionId" value="@Model.Id" />
    <input name="Name" placeholder="Name" />
    <input name="Role" placeholder="Role" />
    <input name="PhotoUrl" placeholder="Photo URL" />
    <textarea name="Bio" placeholder="Bio"></textarea>
    <button type="submit">Add</button>
</form>

<a asp-action="Edit">Edit About Us</a>
